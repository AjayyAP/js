//function declration is one of the ways to define function

//syntax:
// function functionname(){

// function body
// }

      function greet(name){
        console.log("hellow", + name);
        
      }
          greet("ajay");

          //----------- key features-----------
           //* hoisting  ->  calling a functio before defining it
           // * named functions ->  function declaration must have a function name
           // *global or local scope -> its defines outsides any block ,its globally scoped.if defined inside a block its locally scoped
